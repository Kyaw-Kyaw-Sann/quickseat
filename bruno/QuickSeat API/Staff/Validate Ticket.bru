meta {
  name: Validate Ticket
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/staff/tickets/validate
  body: json
}

headers {
  Authorization: Bearer {{staffToken}}
  Content-Type: application/json
}

body:json {
  {
    "ticketToken": "{{ticketToken}}"
  }
}
